# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_USER=ojs
DB_PASSWORD=ojs_secure_password_change_me
DB_DATABASE=ojs
DB_ROOT_PASSWORD=root_secure_password_change_me

# Application Configuration
APP_URL=https://votre-domaine.com
APP_ENV=production

# Email Configuration
# Options: log (dev), smtp (production), sendmail (if available)
SMTP_METHOD=log

# SMTP Settings (for production)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_AUTH=On
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-email-password
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=noreply@example.com
SMTP_AUTH_MECHANISM=PLAIN

# Common SMTP providers examples:
# Gmail: smtp.gmail.com:587 with tls
# Outlook: smtp-mail.outlook.com:587 with tls  
# SendGrid: smtp.sendgrid.net:587 with tls
# Mailgun: smtp.mailgun.org:587 with tls

# File Upload Limits
MAX_FILE_SIZE=100M
MAX_EXECUTION_TIME=300
MEMORY_LIMIT=512M